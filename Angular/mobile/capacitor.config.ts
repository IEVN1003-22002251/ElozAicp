import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aicp.mobile',
  appName: 'AICP Mobile',
  webDir: 'dist/aicp-angular-frontend'
};

export default config;
